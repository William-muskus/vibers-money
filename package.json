{
  "name": "vibers-money",
  "version": "0.1.0",
  "description": "Launch any business with a swarm of AI agents — CEO, directors, and specialists that run 24/7. Next.js, Mistral Vibe, Stripe.",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev:orchestrator": "npm run dev -w @vibers/orchestrator",
    "dev:swarm-bus": "npm run dev -w @vibers/swarm-bus-mcp",
    "dev:computer-use": "npm run dev -w @vibers/computer-use-mcp",
    "dev:frontend": "npm run dev -w @vibers/frontend",
    "dev:all-windows": "powershell -ExecutionPolicy Bypass -File ./scripts/dev-all.ps1",
    "build:inference": "cd packages/inference-engine && cargo build --release --features candle",
    "build:inference-gpu": "node scripts/build-inference-gpu.js",
    "dev:inference": "cd packages/inference-engine && cargo run --release --features candle -- --config configs/roles.toml --port 8080 --log-json",
    "dev:inference-gpu": "node scripts/dev-inference-gpu.js",
    "dev:inference-cpu": "cd packages/inference-engine && cargo run --release --features candle -- --config configs/roles.toml --port 8080"
  },
  "engines": {
    "node": ">=18"
  }
}
