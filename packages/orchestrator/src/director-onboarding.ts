/**
 * First-turn prompt for directors / specialists (spawn only).
 */
export const DIRECTOR_FIRST_CYCLE_SECTION = `--- First cycle ---
(1) \`create-skill\` / \`write_file\` 1–3 skills under \`.vibe/skills/\` for this mission
(2) \`todo_add\` 3–7 todos (create-todo-task)
(3) \`swarm_check_inbox\`, then work todos; report to manager
---`;
